# Dashboard 重设计还原验证

final result: passed

日期：2026-09-18。范围是还原提供的 UI，并按用户补充要求接入“向指挥官留言”；不扩展其他业务能力。

## 设计与实装证据

- 设计说明：`/Users/njugray/projects/design/cmdr/redesign/README.md`。
- 视觉来源：同目录 `project/Main.dc.html`、`TaskDetail.dc.html`、`ConfirmStates.dc.html`、`States.dc.html`。主页面的编辑器模板在临时目录求值后渲染，未修改原稿。
- 最终实装来自 `npm run build` 产物，通过真实 `cmdr dashboard --no-open` 打开。数据在独立的临时 `CMDR_HOME` 中，未访问默认协作目录。
- 截图随仓库保存在 [`.github/assets/dashboard/`](.github/assets/dashboard/)，不进入 npm 发布包。
- 全图对照：[原稿](.github/assets/dashboard/reference-main.jpg)与[实装主视图](.github/assets/dashboard/main.jpg)，均为 1440 × 900 像素，CSS 视口 1440 × 900，按输出像素与 CSS 像素 1:1 比较，没有后期缩放。
- 详情对照：[详情原稿](.github/assets/dashboard/reference-detail.jpg)与[实装详情](.github/assets/dashboard/detail.jpg)，均为 1440 × 900 截图；源稿的 832px 工作区从 x=0 开始，实装相同区域从 x=200 开始。比较时按此偏移定位，未把源稿右边空白算作页面区域。
- [处理回执截图](.github/assets/dashboard/handled.jpg)记录已处理答复及展开的提交证据，采集于隔离 HTTP 测试服务，使用相同组件。
- 两组源图与实装图分别放在同一次图像输入中比较。确认表单、状态标签、底部表格和详情区在原始尺寸全图中逐项检查；文字可辨认，未依赖缩略图做字体判断。浏览器区域裁图出现额外缩小，未用该裁图判断像素精度。

主视图使用与原稿一致的任务标题和主要问题，选择第一项并填写相同补充说明。动态 ID、时间、成员连接、未完成数量和执行记录来自真实队列操作，并未伪造为原稿中的示意状态。

## 对照与修正记录

1. 第一轮发现并修正：
   - [P2] 品牌栏受全局行高影响，比原稿偏高约 6px。将品牌栏行高恢复为 normal；最终高度为 54.7px，导航起点回到原稿位置。
   - [P2] HTML 预览高度略小，示例内容产生多余滚动条。将默认 iframe 高度从 164 调整到 168px，最终示例无需纵向滚动；长内容仍可滚动或扩大查看，不新增 iframe 状态同步机制。
   - [P2] 详情右侧记录区受全局 border-box 影响，比源稿实际外宽少 29px。将外宽调整到 329px，对齐源稿 300px 内容宽加内边距及边框。
2. 修正后重新构建并采集最终主视图与详情图，与源稿再次成组对照。三栏实际宽度为 200 / 832 / 408px，工作区页头 56px，底部坞 180px。上述问题已消除，没有待修正的 P0/P1/P2 项。

## 五项视觉检查

| 检查面 | 结果 |
| --- | --- |
| 字体 | 沿用 Alibaba PuHuiTi 与 JetBrains Mono 字体栈；设计未提供字体文件，实装使用本机 system-ui / Menlo 回退，不增加外部字体请求。源图对照同样使用本机回退。18px 页标题、13px 任务标题、12px 表单、11px 辅助文字和 500 字重层级一致。 |
| 布局与间距 | 三栏、四列看板、独立滚动区、常驻确认面板、底部坞、详情两列、8px 卡片圆角与 36px 主按钮已还原。HTML 使用固定高度沙箱容器，表单末端相较静态稿有约十像素纵向差异，属 P3，不遮挡控制项。 |
| 颜色与令牌 | 白底、暖石选中态、黑色执行中、浅绿完成态、红色阻塞、蓝色已读与链接、珊瑚待回复点均取自设计值。无额外渐变和阴影。 |
| 图像与图标 | 设计无照片或插画；直接沿用原稿的字母 c 标记及返回箭头 SVG，没有新增替代装饰。HTML 示例作为实际 artifact 在沙箱中渲染。 |
| 文案与内容 | 用户留言明确说明不直接修改任务；发送成功仅表示入队。连接、接单、答复接收、指挥官处理分开表达。动态文本可换行或截断并提供完整 title。 |

## 交互与验证

- 浏览器验证切换三个小队；问题选项、答复文本及留言草稿按小队保留。
- 打开任务详情、返回看板、切换成员／活动不卸载常驻答复表单。成员／活动支持方向键及 Home/End 导航和可见焦点。
- 通过 daemon 更新任务，SSE 刷新后输入焦点、文字、选项及 iframe 地址保持不变；React 测试同时断言节点身份不变。
- 问题升版后提交禁用、草稿保留；核对后提交成功，可见接收时间；显式 handle 后展示处理说明，并能读取提交时的 HTML 证据。
- 留言实际写入 `squad:<id>`，来源 user、类型 info，attention=true；由指挥官 read 读取。自动化覆盖鉴权、来源、禁止伪造身份、长度、重复提交、内容冲突、队列满回滚、无指挥官时保留及接管后读取。
- 验证 1280 × 720、900 × 900 和 390 × 844 的布局。窄屏确认面板位于工作区下方，看板与成员表格在各自容器内横向滚动；DOM 几何检查没有文档级横向溢出。窄屏截图工具出现缩放失真，未作移动端像素级一致性的声明。
- 浏览器控制台检查未发现 error / warn。保留 label、fieldset、原生按钮和表格语义；没有新增动画。不声称完成独立 WCAG 审计。
- 最终 `npm run check` 通过：格式、类型、构建、17 个测试文件共 142 项测试及 npm 包离线安装／9 个 MCP 工具验证。
- `git diff --check` 通过。发布包大小阈值提示仍存在；本次未发布或升级已安装插件。

## 约束与可接受差异

- 不实现设计稿中没有业务数据支撑的“查看变更”差异页、重连次数、虚构执行时间线或成员数摘要字段。已有更早执行记录分页保留，实际有记录时才显示按钮。
- 原始 HTML 保留 sandbox 隔离，不为静态稿效果将其注入父页面。普通数据更新保持 DOM，HTML 版本变化仍会重新加载对应 iframe。
- 留言沿用普通消息保留期；只在原消息保留期间对同一提交 ID 去重，不新增永久回执表或 exactly-once 保证。
- 真实 Claude/Codex/ZCode 模型唤醒不在本次浏览器验证中；这里只复用现有机制，不把消息入队或监听健康等同于模型已经处理。

完成项：布局还原、状态与交互、受限留言入口、操作文档、构建与测试、实装截图。
